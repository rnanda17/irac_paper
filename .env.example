# Copy this file to `.env` and fill in your key.
# The notebook loads it via python-dotenv (load_dotenv()).

# OpenRouter API key (https://openrouter.ai/keys)
OPENROUTER_API_KEY=your-openrouter-api-key-here
